import styled from "styled-components";

interface arduinoProps {}

const Wrap = styled.div`
  display: flex;
  margin: 3rem 8rem;
`;

export default function Arduino({}: arduinoProps) {
  return <Wrap>Hellow</Wrap>;
}
